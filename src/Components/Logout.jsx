import React from 'react';

const Logout = ({ logout }) => {
    console.log(logout)
    return (
        <div>
            <button type='submit'>Logout</button>
        </div >
    );
}

export default Logout;
